// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: eval_case.sql

package sqlcgen

import (
	"context"
	"time"
)

const deleteEvalCase = `-- name: DeleteEvalCase :exec
DELETE FROM evidence_eval_case WHERE evidence_id = $1
`

func (q *Queries) DeleteEvalCase(ctx context.Context, evidenceID string) error {
	_, err := q.db.Exec(ctx, deleteEvalCase, evidenceID)
	return err
}

const getEvalCase = `-- name: GetEvalCase :one
SELECT evidence_id, note, created_at, updated_at FROM evidence_eval_case WHERE evidence_id = $1
`

func (q *Queries) GetEvalCase(ctx context.Context, evidenceID string) (EvidenceEvalCase, error) {
	row := q.db.QueryRow(ctx, getEvalCase, evidenceID)
	var i EvidenceEvalCase
	err := row.Scan(
		&i.EvidenceID,
		&i.Note,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertEvalCaseScore = `-- name: InsertEvalCaseScore :exec
INSERT INTO evidence_eval_case_score (evidence_id, competency_id, level) VALUES ($1, $2, $3)
`

type InsertEvalCaseScoreParams struct {
	EvidenceID   string
	CompetencyID string
	Level        int32
}

func (q *Queries) InsertEvalCaseScore(ctx context.Context, arg InsertEvalCaseScoreParams) error {
	_, err := q.db.Exec(ctx, insertEvalCaseScore, arg.EvidenceID, arg.CompetencyID, arg.Level)
	return err
}

const listEvalCaseScores = `-- name: ListEvalCaseScores :many
SELECT evidence_id, competency_id, level FROM evidence_eval_case_score WHERE evidence_id = $1
`

func (q *Queries) ListEvalCaseScores(ctx context.Context, evidenceID string) ([]EvidenceEvalCaseScore, error) {
	rows, err := q.db.Query(ctx, listEvalCaseScores, evidenceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []EvidenceEvalCaseScore
	for rows.Next() {
		var i EvidenceEvalCaseScore
		if err := rows.Scan(&i.EvidenceID, &i.CompetencyID, &i.Level); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listEvalCaseScoresForEvidences = `-- name: ListEvalCaseScoresForEvidences :many
SELECT evidence_id, competency_id, level FROM evidence_eval_case_score
WHERE evidence_id = ANY($1::uuid[])
`

func (q *Queries) ListEvalCaseScoresForEvidences(ctx context.Context, evidenceIds []string) ([]EvidenceEvalCaseScore, error) {
	rows, err := q.db.Query(ctx, listEvalCaseScoresForEvidences, evidenceIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []EvidenceEvalCaseScore
	for rows.Next() {
		var i EvidenceEvalCaseScore
		if err := rows.Scan(&i.EvidenceID, &i.CompetencyID, &i.Level); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listEvalCasesByEvidences = `-- name: ListEvalCasesByEvidences :many
SELECT evidence_id, note, created_at, updated_at FROM evidence_eval_case
WHERE evidence_id = ANY($1::uuid[])
`

func (q *Queries) ListEvalCasesByEvidences(ctx context.Context, evidenceIds []string) ([]EvidenceEvalCase, error) {
	rows, err := q.db.Query(ctx, listEvalCasesByEvidences, evidenceIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []EvidenceEvalCase
	for rows.Next() {
		var i EvidenceEvalCase
		if err := rows.Scan(
			&i.EvidenceID,
			&i.Note,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listEvalCasesForExport = `-- name: ListEvalCasesForExport :many
SELECT ec.evidence_id, ec.note, ec.created_at,
    e.goal_id, e.user_id, e.action_id, e.kind, e.title, e.body,
    e.external_url, e.language
FROM evidence_eval_case ec
JOIN evidence e ON e.id = ec.evidence_id
ORDER BY ec.created_at ASC
`

type ListEvalCasesForExportRow struct {
	EvidenceID  string
	Note        string
	CreatedAt   time.Time
	GoalID      string
	UserID      string
	ActionID    *string
	Kind        string
	Title       *string
	Body        *string
	ExternalUrl *string
	Language    *string
}

// Todos os casos marcados, mais antigo primeiro — é a ordem que numera os
// arquivos exportados (04-agentes.md §6.1).
func (q *Queries) ListEvalCasesForExport(ctx context.Context) ([]ListEvalCasesForExportRow, error) {
	rows, err := q.db.Query(ctx, listEvalCasesForExport)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListEvalCasesForExportRow
	for rows.Next() {
		var i ListEvalCasesForExportRow
		if err := rows.Scan(
			&i.EvidenceID,
			&i.Note,
			&i.CreatedAt,
			&i.GoalID,
			&i.UserID,
			&i.ActionID,
			&i.Kind,
			&i.Title,
			&i.Body,
			&i.ExternalUrl,
			&i.Language,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const replaceEvalCaseScores = `-- name: ReplaceEvalCaseScores :exec
DELETE FROM evidence_eval_case_score WHERE evidence_id = $1
`

func (q *Queries) ReplaceEvalCaseScores(ctx context.Context, evidenceID string) error {
	_, err := q.db.Exec(ctx, replaceEvalCaseScores, evidenceID)
	return err
}

const upsertEvalCase = `-- name: UpsertEvalCase :exec
INSERT INTO evidence_eval_case (evidence_id, note, created_at, updated_at)
VALUES ($1, $2, $3, $3)
ON CONFLICT (evidence_id) DO UPDATE SET note = EXCLUDED.note, updated_at = EXCLUDED.updated_at
`

type UpsertEvalCaseParams struct {
	EvidenceID string
	Note       string
	CreatedAt  time.Time
}

// Marcar de novo só atualiza a nota — não é imutável como evidence (RN-06
// é sobre a evidência em si, não sobre a curadoria de eval em cima dela).
func (q *Queries) UpsertEvalCase(ctx context.Context, arg UpsertEvalCaseParams) error {
	_, err := q.db.Exec(ctx, upsertEvalCase, arg.EvidenceID, arg.Note, arg.CreatedAt)
	return err
}
