# SPA sem SSR: qualquer rota que não bate em arquivo cai no index.html e o
# React Router decide (06-frontend.md §1).
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
